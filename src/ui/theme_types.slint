export struct SlintThemeColors {
    primary-bg: color,
    secondary-bg: color,
    accent: color,
    text-primary: color,
    text-secondary: color,
    border: color,
    menu-bg: color,
    toolbar-bg: color,
    status-bg: color,
    editor-bg: color,
    title-bg: color,
    ribbon-bg: color,
    dropdown-bg: color,
}
