// Independent Slint Hierarchy Tool Window
import {
    Button,
    TextEdit,
    ScrollView,
    HorizontalBox,
    VerticalBox,
} from "std-widgets.slint";
import { ToolbarButton } from "components/toolbar_button.slint";
import { Theme } from "styles.slint";

export component HierarchyWindow inherits Window {
    title: "Hierarchy Tool - Herding Cats";
    callback set-theme(string);
    set-theme(theme-name) => {
        if (theme-name == "Normal") {
            Theme.primary-bg = #ffffff;
            Theme.secondary-bg = #f8f9fa;
            Theme.accent = #007bff;
            Theme.text-primary = #212529;
            Theme.text-secondary = #6c757d;
            Theme.border = #dee2e6;
            Theme.menu-bg = #f8f9fa;
            Theme.toolbar-bg = #ffffff;
            Theme.status-bg = #e9ecef;
            Theme.editor-bg = #ffffff;
            Theme.title-bg = #e9ecef;
            Theme.ribbon-bg = #f5f5f5;
            Theme.dropdown-bg = #ffffff;
        } else if (theme-name == "Warm") {
            Theme.primary-bg = #fdf6e3;
            Theme.secondary-bg = #eee8d5;
            Theme.accent = #cb4b16;
            Theme.text-primary = #586e75;
            Theme.text-secondary = #93a1a1;
            Theme.border = #d3cbb7;
            Theme.menu-bg = #eee8d5;
            Theme.toolbar-bg = #fdf6e3;
            Theme.status-bg = #eee8d5;
            Theme.editor-bg = #fdf6e3;
            Theme.title-bg = #eee8d5;
            Theme.ribbon-bg = #fdf6e3;
            Theme.dropdown-bg = #fdf6e3;
        } else if (theme-name == "CRT Green") {
            Theme.primary-bg = #0d1117;
            Theme.secondary-bg = #161b22;
            Theme.accent = #00ff00;
            Theme.text-primary = #00ff00;
            Theme.text-secondary = #008f00;
            Theme.border = #30363d;
            Theme.menu-bg = #161b22;
            Theme.toolbar-bg = #0d1117;
            Theme.status-bg = #161b22;
            Theme.editor-bg = #0d1117;
            Theme.title-bg = #161b22;
            Theme.ribbon-bg = #0d1117;
            Theme.dropdown-bg = #161b22;
        } else if (theme-name == "CRT Amber") {
            Theme.primary-bg = #1a1a1a;
            Theme.secondary-bg = #2b2b2b;
            Theme.accent = #ffb000;
            Theme.text-primary = #ffb000;
            Theme.text-secondary = #b37b00;
            Theme.border = #4d4d4d;
            Theme.menu-bg = #2b2b2b;
            Theme.toolbar-bg = #1a1a1a;
            Theme.status-bg = #2b2b2b;
            Theme.editor-bg = #1a1a1a;
            Theme.title-bg = #2b2b2b;
            Theme.ribbon-bg = #1a1a1a;
            Theme.dropdown-bg = #2b2b2b;
        }
    }

    callback close_requested();
    callback new_item();
    callback delete_item();
    callback move_up();
    callback move_down();
    
    // Window styling
    property <color> title-bar-color: Theme.title-bg;
    property <color> background-color: Theme.secondary-bg;
    
    // Main content
    Rectangle {
        background: title-bar-color;
        height: 35px;
        y: 0;
        width: parent.width;
        HorizontalBox {
            padding: 10px;
            spacing: 10px;
            Text {
                text: "📊 Document Hierarchy";
                color: Theme.text-primary;
                font-size: 14px;
                font-weight: 600;
                vertical-alignment: center;
            }

            Rectangle { }
        }
    }
    
    // Content area
    Rectangle {
        y: 35px;
        height: parent.height - 35px;
        width: parent.width;
        background: background-color;
        VerticalBox {
            padding: 15px;
            spacing: 15px;
            Text {
                text: "📊 Document Hierarchy - RESIZEABLE!";
                font-size: 18px;
                font-weight: 700;
                color: Theme.text-primary;
            }
            
            // Hierarchy tree
            ScrollView {
                height: 350px;
                VerticalBox {
                    spacing: 8px;
                    Rectangle {
                        background: Theme.primary-bg;
                        border-radius: 8px;
                        padding: 12px;
                        VerticalBox {
                            spacing: 4px;
                            Text {
                                text: "📖 ACT I - SETUP";
                                font-size: 14px;
                                font-weight: 600;
                                color: #2c3e50;
                            }

                            Text {
                                text: "Chapter 1: Introduction - Character establishment and world building";
                                font-size: 12px;
                                color: Theme.text-secondary;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 2: Background - Past events that lead to the main story";
                                font-size: 12px;
                                color: Theme.text-secondary;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 3: Inciting Incident - Event that starts the main plot";
                                font-size: 12px;
                                color: Theme.text-secondary;
                                wrap: word-wrap;
                            }
                        }
                    }

                    Rectangle {
                        background: white;
                        border-radius: 8px;
                        padding: 12px;
                        VerticalBox {
                            spacing: 4px;
                            Text {
                                text: "📈 ACT II - DEVELOPMENT";
                                font-size: 14px;
                                font-weight: 600;
                                color: #2c3e50;
                            }

                            Text {
                                text: "Chapter 4: Rising Action - Building tension and complications";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 5: Complications - Obstacles and challenges increase";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 6: Midpoint Crisis - Major turning point in the story";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }
                        }
                    }

                    Rectangle {
                        background: white;
                        border-radius: 8px;
                        padding: 12px;
                        VerticalBox {
                            spacing: 4px;
                            Text {
                                text: "🎭 ACT III - RESOLUTION";
                                font-size: 14px;
                                font-weight: 600;
                                color: #2c3e50;
                            }

                            Text {
                                text: "Chapter 7: Climax - The final confrontation and highest tension";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 8: Falling Action - Consequences and resolution begin";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }

                            Text {
                                text: "Chapter 9: Resolution - Final conclusions and new equilibrium";
                                font-size: 12px;
                                color: #7f8c8d;
                                wrap: word-wrap;
                            }
                        }
                    }
                }
            }
            
            // Controls
            HorizontalBox {
                spacing: 10px;
                ToolbarButton {
                    text: "➕ Add Chapter";
                    width: 120px;
                    height: 30px;
                }

                ToolbarButton {
                    text: "✏️ Edit";
                    width: 80px;
                    height: 30px;
                }

                ToolbarButton {
                    text: "🗑️ Delete";
                    width: 80px;
                    height: 30px;
                }
            }

            HorizontalBox {
                spacing: 10px;
                ToolbarButton {
                    text: "⬆️ Move Up";
                    width: 80px;
                    height: 25px;
                }

                ToolbarButton {
                    text: "⬇️ Move Down";
                    width: 80px;
                    height: 25px;
                }

                ToolbarButton {
                    text: "🔄 Reorganize";
                    width: 100px;
                    height: 25px;
                }
            }
        }
    }
    
    // VISUAL RESIZE HANDLES
    // Bottom-right corner resize handle
    Rectangle {
        x: parent.width - 12px;
        y: parent.height - 12px;
        width: 12px;
        height: 12px;
        background: #4a90e2;
        border-radius: 1px;
        drop-shadow-blur: 2px;
    }
    
    // Right edge resize handle
    Rectangle {
        x: parent.width - 2px;
        y: 35px;
        width: 2px;
        height: parent.height - 35px - 12px;
        background: #4a90e2;
    }
    
    // Bottom edge resize handle
    Rectangle {
        x: 12px;
        y: parent.height - 2px;
        width: parent.width - 12px - 12px;
        height: 2px;
        background: #4a90e2;
    }
    
    // Left edge resize handle
    Rectangle {
        x: 0px;
        y: 35px;
        width: 2px;
        height: parent.height - 35px - 12px;
        background: #4a90e2;
    }
    
    // Top edge resize handle
    Rectangle {
        x: 12px;
        y: 0px;
        width: parent.width - 12px - 12px;
        height: 2px;
        background: #4a90e2;
    }
}
